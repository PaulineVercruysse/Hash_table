def make_hashtable(nbuckets):
    table = [ ]
    for i in range(0, nbuckets): #use built-in Python function "range"
        table.append([ ]) #had a new empty list (bucket) to the table
   return table